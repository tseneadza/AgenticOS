# Session log — 2026-06-17

Live-verification session for Phase 10 (NF-3). Closed all four original open
issues and shipped several fixes found while testing the running app.

## What we did

### Closed the four queued issues
- **#1 Send-to-unavailable-model** — `AgentView` gates Send/textarea on the active
  model's availability; auto-falls-back to an available local model; inline hint.
- **#2 Cloud "Connection error"** — root-caused with `scripts/diagnose_cloud.py`:
  the shell exports `ANTHROPIC_BASE_URL=http://localhost:12434` +
  `ANTHROPIC_AUTH_TOKEN=ollama`, which the anthropic SDK inherited, so "cloud"
  calls hit local Ollama (404 / connection error). Fixed by pinning the endpoint
  (`anthropic_base_url()` + `_isolated_anthropic_env()` in `core/llm.py`).
- **#3 Small-model tool-calling** — tool-first `GOVERNOR_SYSTEM` + dynamic Ollama
  discovery, `ollama serve` auto-start, and RAM-gating in `core/llm.py`. Probe
  (`scripts/diagnose_tools.py`) confirmed qwen2.5-7B AND claude-sonnet-4-6 both
  bind tools and emit `tool_calls` — no capability/binding bug.
- **#4 Commit mislabel** — history note added (commit `0270602` = 10a+10b+10c).

### Live-found fixes shipped
- **Persistent chat transcript** — chat was "refreshing" each turn because it was
  re-derived from the 200-event-capped feed and token-streaming overran it. Now
  accumulated in App state (`agentTurns` / `foldAgentEvent`). Confirmed live.
- **Tool-manifest hardening** — bound-tool roster injected into the system prompt.
- **Soul + Memory** — persistent identity ("Osa") + durable memory in
  `config/Soul.md` / `config/Memory.md`, loaded into all LLM-facing agents via
  `core/soul.py`; automatic `remember` tool. Migrated YAML → Markdown.
- **`run_shell`** — agent runs terminal commands inline; allowlist-auto,
  approve-the-rest (HITL), blocked-patterns refused, runs in `~`.
- **Native Edit menu** — fixes paste/copy/cut in Tauri text inputs.

## Commits
- `1fd38dc` (pushed) — issues #1–#4 bundle.
- `00282f3` (pushed) — transcript + tool manifest + Soul/Memory + diagnostics.
- `a07fa50` (local, pending push) — `run_shell` + Edit menu.

## Next-day focus
1. Push `a07fa50`; tidy `.git/_sl_*` / `_stalelock*` / `index.lock` leftovers and
   dead `config/Soul.yaml` / `Memory.yaml` on the Mac.
2. Restart sidecar + `npm run tauri dev` rebuild, then live-verify: `run_shell`
   (auto `ls` + approval prompt for a mutating command), Cmd+V paste, Soul/Memory
   ("what's your name?" / "remember that …").
3. Begin **Phase 9 (Hub absorption)** — the last remaining roadmap build.

## Gotchas reinforced this session
- The sandbox mount blocks git's `unlink` (so `.git/index.lock` can't be deleted)
  but allows `rename` — commits from the sandbox use plumbing + a renamed lock;
  pushes must happen on the Mac.
- Restart the sidecar to pick up Python changes; rebuild Tauri for `lib.rs`/Rust.
