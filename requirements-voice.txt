# OSA voice pipeline — OPTIONAL extras (Phase 14d). NOT part of the base
# requirements: the sidecar runs fully without these; `osa_voice` imports them
# lazily and reports what's missing via voice_available().
#
# Install on-device into the existing venv:
#     .venv/bin/pip install -r requirements-voice.txt
#
# Notes:
#   * macOS will prompt for microphone permission on first live run (grant it
#     to the terminal/sidecar host process). VOICE-OUT (piper-tts) needs NO
#     mic permission — it plays through macOS `afplay` (built-in).
#   * On-device only (design doc §10) — the assistant sandbox can't run
#     mic/audio; Tony verifies the pipeline on his Mac.
#   * All processing is local/offline (decision #3): no cloud STT/TTS.
#
# STATUS 2026-07-08: piper-tts is INSTALLED (voice-OUT is live). The four
# mic-side deps below remain uninstalled until the voice-IN pass.
#   * piper-tts pulls onnxruntime + bumps numpy to 2.x — full suite stays green.
#   * Voice model en_GB-alan-medium lives in ~/.agentic-os/voices/ (fetched via
#     `python -m piper.download_voices en_GB-alan-medium --download-dir ...`).
openwakeword          # wake word ("osa") — rolling buffer detection (§3.1) [voice-IN, not yet installed]
faster-whisper        # local STT, model size via constitution voice.stt_model (§3.2) [voice-IN, not yet installed]
piper-tts             # local TTS; voice model via voice.piper_voice (§3.3) [voice-OUT, INSTALLED 2026-07-08]
sounddevice           # mic capture + speaker playback (PortAudio) [voice-IN, not yet installed]
webrtcvad             # end-of-speech / silence detection [voice-IN, INSTALLED 2026-07-08]
setuptools<81         # webrtcvad 2.0.10 imports pkg_resources, removed in setuptools 81+ (hit 2026-07-08)
