langgraph>=0.4
mcp>=1.0
# LangGraph checkpointer — MySQL (all state lives in the AgenticOS schema).
# The [pymysql] extra pulls the pure-Python PyMySQL driver the saver uses;
# requires MySQL >= 8.0.19 (or MariaDB >= 10.7.1).
langgraph-checkpoint-mysql[pymysql]>=2.0
PyMySQL>=1.1
anthropic>=0.40
pyyaml>=6.0
requests>=2.31
python-dotenv>=1.0
fastapi>=0.110
uvicorn[standard]>=0.29
psutil>=5.9
mysql-connector-python>=8.0
iterm2>=2.6              # Phase 4 FR-08: iTerm2 Python API (macOS only)

# Phase 10 / NF-3 (FR-52): unified LLM provider layer (cloud + local).
langchain-core>=0.3
langchain-anthropic>=0.3
langchain-ollama>=0.2   # talks to a local Ollama service on :11434 (optional)

# Optional provider packages (install only the backends you actually use).
# The adapters in core/llm.py import these lazily, so the OS runs without them.
# langchain-openai>=0.2        # provider: openai_compatible (OpenAI, Groq, OpenRouter, DeepSeek, Mistral, xAI, vLLM, LM Studio, …)
# langchain-google-genai>=2.0  # provider: google (Gemini)
